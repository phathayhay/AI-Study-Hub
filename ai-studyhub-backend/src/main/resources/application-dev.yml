spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/ai_studyhub?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
    username: root
    password: root123

logging:
  level:
    com.studyhub: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG

app:
  frontend-url: http://localhost:5173
